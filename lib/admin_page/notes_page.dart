import 'package:flutter/material.dart';

class NotesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("الملاحظات")),
      body: Center(child: Text("هذه هي صفحة الملاحظات")),
    );
  }
}